extends Sprite


func _ready():
	#var x = self.position.x - self.get_rect().size.x / 2
	#var y = self.position.y - self.get_rect().size.y / 2
	#$TopBoardLabel.set_position(Vector2(x,y))
	#$TopBoardLabel.set_size(Vector2(self.get_rect().size.x,self.get_rect().size.y))
	pass
